---
name: xialingguo-ip
description: 夏林果真人 IP 封面设计 Skill；面向小白收集少量必要信息，自动推荐风格并生成可发布的高清文章封面，支持真人 IP、AI、Web 出海和效率类内容。
metadata:
  short-description: AI 博主文章封面设计模板系统
---

# 夏林果真人 IP 封面设计 Skill

用于为 AI 教程、AI 提效、Web 出海和产品体验文章制作可发布的高清封面。核心原则是“风格固定，主次灵活”：用户选择一种风格，再说明本次要突出标题、真人 IP、产品、流程、结果或故事场景，不能把人物位置误当成风格本身。

## 面向小白的入口

每次调用本 Skill 后，先用一条消息展示一份简短表单，让用户一次看到所有真正必要的信息。不要把问题拆成连续盘问，也不要要求用户懂设计术语。

开场使用下面的格式或同等简洁版本：

> 为了直接做出合适的封面，你可以一次告诉我：
> 1. **文章标题/主题**：完整标题，或文章要讲什么。
> 2. **风格**：从风格库选一个；不确定就说“你帮我选”。
> 3. **视觉重点**：标题、真人、产品/界面、流程、结果或故事，最想让读者先看到什么？
> 4. **真人 IP**：上传哪一张姿势，或暂时不使用真人？
>
> 可选补充：比例（默认 21:9）、产品截图/Logo/数据图、必须保留的文字、希望避开的颜色。

用户可以把不知道的项目留空。收到回答后，自动识别已提供的信息，只补问会实质影响结果的缺失项；最多补问 1–2 项，不重复问已经回答过的内容。没有风格时，直接推荐最匹配的 1–2 种，并允许用户回复“用第一个”。

默认比例为 21:9。用户没有选择姿势时，根据视觉重点推荐姿势。没有产品截图时，用抽象但明确的浏览器窗口、地图、流程和结果元素，不要因此阻塞设计。

信息已经足够时，先用一句话复述“风格 + 视觉重点 + 真人姿势 + 比例”，随后直接生成，不强制再等待一次确认。只有存在明显歧义、标题可能被改写或用户要求高风险外部操作时才暂停确认。用户说“直接做/不用问了”时，按已有信息执行。

## 默认工作方式

1. 读取用户提供的标题、文章主题和素材；如果用户提供的是文章文件，先提炼文章核心卖点、受众和 1–3 个视觉元素。
2. 选择或确认 9 种风格中的一种；若用户未指定，根据文章主题推荐 1–2 种。
3. 判断本次视觉重点：标题主视觉、人物主视觉、产品主视觉、流程主视觉、结果主视觉或故事场景主视觉。
4. 保持选定风格的颜色、字体、质感和装饰语言，但根据视觉重点调整构图和元素主次。
5. 默认横版为 21:9；用户指定其他比例时遵从用户要求。
6. 生成的是可发布的高清封面成品，不是 README 缩略图、低清预览或风格样图。缩略图只用于风格库展示，不能作为交付结果。
7. 生成图片时优先使用用户指定的真人 IP参考图，保持人物身份一致。只有用户明确要求“原图直接放入/抠图”时，才必须保持面部表情、动作、服装和发型不变，不重绘、不换脸、不美化、不加滤镜。
8. 输出前检查：比例是否正确、标题是否完整、主体是否清晰、真人是否被裁切、重点是否突出。发现明显错误时继续修正或明确告知用户需要后期排版，不把错误版本当最终成品交付。

## 真人 IP素材

这是公共 Skill，不预设、也不写死任何人的真人 IP。每位用户都需要提供自己的真人 IP参考图，可以是一张或多张不同姿势的照片，并自行命名为“打招呼版”“AI 讲解版”“举食指版”等。Skill 不应把仓库里的样图误当作用户真人素材。

如果用户没有指定姿势，优先根据视觉重点推荐最合适的一张；如果用户要求保持原样，必须使用用户提供的对应参考图，并明确“保持脸、表情、动作不变”。

## 风格库

详细的 9 种风格、颜色、结构、元素和样图路径见 [references/style-library.md](references/style-library.md)。使用某一种风格时，先读取该参考文件中对应的条目。

## 生成约束

- 标题需要按用户要求完整保留；不要擅自改写标题。
- 中文文字较多时，图片生成模型可能出现错字。默认仍以“可发布封面”为目标；如果标题准确性极其重要，先提醒用户图片模型的文字风险，并建议生成高清视觉底稿后用 Canva、Figma 等工具排版，只有用户同意时才改为底稿模式。
- 不要使用虚构品牌 Logo；用户提供 Logo 或明确要求时才加入。
- 不要把所有封面都做成同一套构图；保持风格语言一致，但让视觉重点决定主次。
- 避免廉价的全息投影、过量粒子、过多发光和无意义的数据流。
